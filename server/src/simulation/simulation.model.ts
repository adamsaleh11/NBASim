import { Injectable } from '@nestjs/common';

@Injectable()
export class SimulationModel {
  // Define properties of your simulation
  id: string;
  name: string;
  // Other properties related to the simulation model
}